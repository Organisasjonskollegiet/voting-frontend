query GetVotationById($id: ID!) {
  votationById(id: $id) {
    id
    title
    description
    hasVoted {
      id
    }
    alternatives {
      id
      text
    }
    status
    blankVotes
  }
}
