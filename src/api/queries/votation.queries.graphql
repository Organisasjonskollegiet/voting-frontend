query GetVotationById($votationId: String!) {
  votationById(votationId: $votationId) {
    id
    title
    description
    hasVoted {
      id
    }
    alternatives {
      id
      text
    }
    status
    blankVotes
  }
}
