query GetMeetings {
  meetings {
    id
    title
    description
    owner {
      id
      email
    }
  }
}

query GetParticipantsByMeeting($meetingId: String!) {
  meetingsById(meetingId: $meetingId){
    participants {
      user {
        email
      }
      role
    }
  }
}
